[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback (also shipping public/_redirects)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
